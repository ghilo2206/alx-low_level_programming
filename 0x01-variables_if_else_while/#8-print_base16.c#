#include <stdio.h>

/**
 * main - Prints all the numbers of base 16 in lowercase.
 *
 * Returnetter;

 for (num = 0; num < 10; num++)
 putchar(: Always 0.
*/
int main(void)
{
  int num;
  char l(num % 10) + '0');

for (letter = 'a'; letter <= 'f'; letter++)
  putchar(letter);

putchar('